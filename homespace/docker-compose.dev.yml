services:
  backend:
    volumes:
      - ./backend:/app
      - /app/node_modules
      - uploads_data:/app/uploads

  web:
    volumes:
      - ./web:/app
      - /app/node_modules
      - /app/.next
